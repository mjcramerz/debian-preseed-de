# Future managed Wi-Fi handoff compatibility marker.
# Static installs now prepare the installed system's Wi-Fi handoff directly from
# cmdline-driven target networking and never associate Wi-Fi during d-i.
d-i hw-detect/load_firmware boolean true
d-i hw-detect/load_firmware seen true
