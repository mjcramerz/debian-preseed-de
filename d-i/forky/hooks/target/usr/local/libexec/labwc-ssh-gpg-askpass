#!/bin/sh
set -eu
umask 077
# shellcheck disable=SC1091
. /usr/local/libexec/ssh-checks
managed_ssh_context || exit 1
exec /usr/bin/gpg --quiet --no-options --no-tty --pinentry-mode ask --decrypt -- "$blob"
