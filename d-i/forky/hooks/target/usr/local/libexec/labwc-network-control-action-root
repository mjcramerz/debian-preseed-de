#!/usr/bin/perl
use strict;
use warnings;

use lib '/usr/local/lib/perl5/site_perl/labwc-network-control-action';

use LabwcNetworkControlAction::Root;

exit LabwcNetworkControlAction::Root->new()->run(@ARGV);
