#!/bin/sh
# No arguments accepted; all recording control remains in the existing worker.
[ "$#" -eq 0 ] || exit 64
exec /usr/bin/timeout --foreground --signal=INT --kill-after=1s 15s /usr/local/libexec/whisper-record-toggle record-worker
