#!/bin/sh
# The recorder supervises pw-record directly so an intentional SIGINT can be
# distinguished from a real capture failure. Do not wrap it in GNU timeout.
[ "$#" -eq 0 ] || exit 64
exec /usr/local/libexec/whisper-record-toggle record-worker
