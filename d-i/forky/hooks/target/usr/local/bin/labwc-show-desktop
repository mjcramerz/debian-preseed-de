#!/bin/sh
set -eu

if ! command -v wlrctl >/dev/null 2>&1; then
  exit 0
fi

wlrctl toplevel minimize state:inactive >/dev/null 2>&1 || true
wlrctl toplevel minimize state:active >/dev/null 2>&1 || true
