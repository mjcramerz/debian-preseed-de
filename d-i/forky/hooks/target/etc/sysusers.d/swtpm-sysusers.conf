# Managed by unattended-installer.
#
# swtpm and tpm2-tss both reference the shared tss account on Debian. Keep the
# vendor swtpm sysusers entry masked so systemd-sysusers does not emit a
# duplicate-account conflict on systems where the qemu addon installs swtpm.
