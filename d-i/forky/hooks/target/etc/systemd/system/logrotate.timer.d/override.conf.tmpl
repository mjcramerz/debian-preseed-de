[Timer]
# Replace the vendor daily schedule with frequent bounded-growth checks.
OnCalendar=
OnCalendar=*-*-* *:00/__INSTALLER_LOG_ROTATE_CHECK_MINUTES__:00

# Stagger hosts while keeping the maximum scheduling delay small.
AccuracySec=1m
RandomizedDelaySec=2m
Persistent=true
