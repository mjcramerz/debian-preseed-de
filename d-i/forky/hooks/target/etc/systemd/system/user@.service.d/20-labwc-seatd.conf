# Order every user manager after seatd so reverse shutdown ordering stops user
# services before the seat daemon. Labwc uses the seatd backend.
[Unit]
# Reverse shutdown ordering also keeps BlueZ available while user audio and
# tray clients disconnect. This orders an existing daemon; it does not pull it in.
After=seatd.service bluetooth.service
