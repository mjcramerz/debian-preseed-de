# Managed by unattended-installer.
# Let systemd enable the ancestor controllers and own the cgroup hierarchy.
# Delegation is independent of accounting defaults and optional I/O weights.
# Do not add CPU affinity, a cpuset partition, or resource ceilings here.
[Service]
Delegate=
Delegate=cpuset cpu pids memory io
