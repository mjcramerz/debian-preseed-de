# Managed by unattended-installer.
#
# The upstream user manager unit is intentionally easier to kill than system
# services. Development workloads must not inherit that positive OOM bias.
[Service]
OOMScoreAdjust=0
UMask=0077
