[Service]
# A skipped ExecCondition is not a daemon failure and therefore does not feed
# tailscaled's restart loop. The coordinator records a blocked start request so
# Mullvad's stop path restores the requested Tailscale state.
ExecCondition=/usr/local/libexec/mullvad-tailscale-coordinator tailscale-start
