# Managed by unattended-installer.
#
# Debian's unattended-upgrade-shutdown helper can emit a PyGObject deprecation
# warning for GLib.unix_signal_add during shutdown. Keep shutdown behavior
# unchanged and suppress deprecation-only noise from this package-owned helper.

[Service]
Environment=PYTHONWARNINGS=ignore::DeprecationWarning
