# Keep the vendor lifecycle and firewall ordering. Disable its duplicate file
# sink; the journal collector routes this identifier using logging.env.
[Service]
ExecStart=
ExecStart=/usr/bin/mullvad-daemon -vv --disable-stdout-timestamps --disable-log-to-file
StandardOutput=journal
StandardError=journal
SyslogIdentifier=mullvad-daemon
