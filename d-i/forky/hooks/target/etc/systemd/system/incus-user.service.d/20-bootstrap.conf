[Unit]
# Keep the daemon, managed host state, and package-owned shutdown helper
# demand-triggered by the restricted user socket rather than boot or login.
Requires=incus-host.service
After=incus-host.service
