# Managed by unattended-installer.
# Share the class budget. Preserve this service's nice level, I/O scheduler,
# timeout, sandbox, AppArmor transitions and notification hooks.
[Service]
Slice=system-background.slice
