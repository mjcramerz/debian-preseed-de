[Unit]
# /var/cache is tmpfs-backed on managed installs. Ensure tmpfiles has
# recreated the parser cache before the early AppArmor load begins.
After=systemd-tmpfiles-setup.service

[Service]
ExecStartPre=/usr/bin/install -d -o root -g root -m 0755 /var/cache/apparmor
