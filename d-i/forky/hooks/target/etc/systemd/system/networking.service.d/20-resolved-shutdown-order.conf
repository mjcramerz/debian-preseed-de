[Unit]
# Debian's if-down.d/resolved hook calls resolvectl while networking.service
# stops. Keep both the resolver and its D-Bus transport available until that
# hook has completed; reverse ordering stops networking.service first.
After=dbus.service systemd-resolved.service
