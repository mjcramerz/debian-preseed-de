[Unit]
# The late-stage renderer removes a resolved Before=networking.service edge
# when one exists. Add the reciprocal startup order so ifupdown and
# wpa_supplicant remain available during NetworkManager shutdown.
Wants=NetworkManager-dispatcher.service
After=NetworkManager-dispatcher.service networking.service wpa_supplicant.service
