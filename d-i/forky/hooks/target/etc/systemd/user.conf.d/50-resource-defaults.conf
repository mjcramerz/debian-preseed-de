# Managed by unattended-installer.
#
# Keep user services neutral for OOM selection and do not impose systemd's
# finite per-unit task default on interactive build descendants.
[Manager]
DefaultOOMScoreAdjust=0
DefaultTasksMax=infinity
