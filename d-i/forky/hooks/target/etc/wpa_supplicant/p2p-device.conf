# Managed by unattended-installer.
#
# Explicit P2P Device config for the system D-Bus supplicant. The managed
# hosts do not use Wi-Fi Direct, and this avoids exposing an unconfigured
# P2PDeviceConfig property path to D-Bus clients.

p2p_disabled=1
