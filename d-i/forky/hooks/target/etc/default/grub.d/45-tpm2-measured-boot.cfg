# Managed crypto policy. Use the distribution's existing TPM module; no builds.
# The release gate requires PCR8 command-line and PCR9 initrd measurement evidence.
GRUB_PRELOAD_MODULES="${GRUB_PRELOAD_MODULES:+${GRUB_PRELOAD_MODULES} }tpm"
