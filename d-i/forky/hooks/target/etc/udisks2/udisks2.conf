[udisks2]
# Keep optional modules available, but load them only when a D-Bus client needs
# them. This preserves normal desktop behavior without increasing daemon
# startup surface.
modules=*
modules_load_preference=ondemand

[defaults]
# Prefer LUKS2 for new encrypted filesystems created through udisks.
encryption=luks2
