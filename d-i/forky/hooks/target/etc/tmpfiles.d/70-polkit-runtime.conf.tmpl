# Managed by unattended-installer.
#
# polkit scans these optional rules directories on startup. Create them so
# startup is quiet even when no local site-specific rules are installed yet.
d __INSTALLER_DIR_POLKIT_RUNTIME_RULES_D__ 0755 root root -
d __INSTALLER_DIR_POLKIT_LOCAL_RULES_D__ 0755 root root -
