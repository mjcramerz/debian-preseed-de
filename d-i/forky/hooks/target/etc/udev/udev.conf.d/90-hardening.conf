# Managed by unattended-installer.
# Keep device-event handling bounded and quiet on unattended hosts.
udev_log=err
children_max=0
exec_delay=0
event_timeout=60
resolve_names=early
timeout_signal=SIGKILL
