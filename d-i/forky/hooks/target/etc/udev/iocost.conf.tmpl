# Managed by unattended-installer: IOCost.
# Native udev currently reads this file, not iocost.conf.d/*.conf.
# Materialized from the same profile as the managed drop-in; do not diverge.
# Device values are profile-specific/provisional until calibrated.
[IOCost]
TargetSolution=__INSTALLER_IOCOST_TARGET_SOLUTION__
