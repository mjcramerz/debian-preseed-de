# Managed by unattended-installer.
# Keep Nouveau disabled on managed installs. The NVIDIA driver modules
# themselves are handled separately by the active NVIDIA policy or the
# non-NVIDIA fallback blacklist.
blacklist nouveau
