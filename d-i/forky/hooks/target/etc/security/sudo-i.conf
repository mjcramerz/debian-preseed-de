# sudo -i is a privileged shell, not a second graphical desktop. Preserve
# normal authentication and register a light root session without user@0.
XDG_SESSION_CLASS DEFAULT=user-early-light OVERRIDE=user-early-light
