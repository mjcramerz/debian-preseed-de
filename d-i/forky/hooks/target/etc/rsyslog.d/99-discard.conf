# Discard classified records from the remaining generic rules after every
# matching numbered destination has run. Unclassified messages continue to the
# general facility rules in /etc/rsyslog.conf.

if ($.managed_security_routed == "on") then {
  stop
}
