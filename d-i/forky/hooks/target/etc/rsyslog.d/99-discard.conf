# Stop classified records after all dedicated destinations have run.
# 44-components.conf assigns every remaining record to one of five
# categories. There is no duplicate legacy facility-file fallback.

if ($.managed_security_routed == "on") then {
  stop
}
