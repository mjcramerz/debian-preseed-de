# Load the actual xxHash implementation once, then the deployed filesystems.
# xxhash64 and xxhash64-generic are aliases of xxhash_generic; the old
# xxhash64_generic spelling is redundant. Use its canonical module name here
# and in initramfs-tools/modules so kmod and initramfs-tools agree.
xxhash_generic
btrfs
xfs
