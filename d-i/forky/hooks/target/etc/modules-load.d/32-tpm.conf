# Managed by unattended-installer.
#
# Load the core TPM drivers early enough for /dev/tpmrm0 to be available to
# systemd, TPM2 tooling, and Secure Boot maintenance helpers on capable hosts.
tpm
tpm_tis
tpm_crb
trusted
encrypted-keys
