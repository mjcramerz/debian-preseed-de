# Managed by unattended-installer.
# The session panel must remain responsive while applications are busy.
# Resource weights live on the parent slice, not on this service.
[Service]
Slice=session.slice
