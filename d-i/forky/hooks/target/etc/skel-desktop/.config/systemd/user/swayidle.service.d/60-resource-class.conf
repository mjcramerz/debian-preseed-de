# Managed by unattended-installer.
# Session idle and lock handling is latency-sensitive infrastructure.
# Resource weights live on the parent slice, not on this service.
[Service]
Slice=session.slice
