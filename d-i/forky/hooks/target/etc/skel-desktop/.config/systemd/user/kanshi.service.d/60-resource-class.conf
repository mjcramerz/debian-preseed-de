# Managed by unattended-installer.
# Output configuration is part of keeping the graphical session usable.
# Resource weights live on the parent slice, not on this service.
[Service]
Slice=session.slice
