# Managed by unattended-installer.
# Prefer compositor work among session.slice siblings without a CPU quota.
# These relative weights do not bypass the parent slice or schedule GPU work.
[Service]
CPUWeight=__SYSTEMD_CPUWEIGHT_HOME_USER_LABWC_COMPOSITOR_SERVICE_D__
__SYSTEMD_IOWEIGHT_HOME_USER_LABWC_COMPOSITOR_SERVICE_D__
