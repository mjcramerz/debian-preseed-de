# Managed by unattended-installer.
# Keep application scopes in the same class as ordinary app services.
# Scope resource controls belong in [Scope]; do not add exec/resource limits
# such as LimitCORE here, or duplicate the parent slice's weights.
[Scope]
Slice=app.slice
