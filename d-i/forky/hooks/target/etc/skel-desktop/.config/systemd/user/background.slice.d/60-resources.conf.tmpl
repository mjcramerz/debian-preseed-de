# Managed by unattended-installer.
# Noninteractive user work; lower preference, not a CPU quota or idle starvation.
# Weights compare sibling slices, not individual services across the tree.
# Accounting is inherited from user.conf.d; no task or memory ceilings here.
[Slice]
__SYSTEMD_CPUWEIGHT_HOME_USER_BACKGROUND_SLICE_D__
__SYSTEMD_IOWEIGHT_HOME_USER_BACKGROUND_SLICE_D__
