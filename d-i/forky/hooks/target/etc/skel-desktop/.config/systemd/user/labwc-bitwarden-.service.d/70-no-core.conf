# Managed by unattended-installer.
# Prefix matches labwc-bitwarden-<uuid>.service, including its children.
# systemd-coredump can still log crash metadata. This is not a ptrace boundary.
[Service]
LimitCORE=0
