[Service]
# Keep success/veto status until the privileged worker explicitly reads it.
# An inactive successful oneshot may otherwise be garbage-collected before
# systemctl show, losing ExecMainStatus=77 and appearing to be a fresh unit.
RemainAfterExit=yes
