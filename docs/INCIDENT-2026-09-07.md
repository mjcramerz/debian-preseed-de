# Sanitized incident inventory

Both supplied logs were scanned chronologically and matched to the supplied ZIP.
This inventory groups repeated instances and routine maintainer-script messages;
it does not reproduce credentials, private paths, device serials or full commands.
Input line numbers refer to the original local text files (not web-rendered lines).
The private raw-match inventory is intentionally excluded from the release.

| Evidence / meaningful issue | Classification | Cause and disposition |
|---|---|---|
| installer.log ~421, 1661, 2991: missing GRUB/MOK variable; customization follows | ROOT CAUSE; CODE FIX; TEST | Shared placeholder map consumed architecture values before family policy initialization. Policy now precedes all renderers; renderer failure stops the family. |
| installer.log late starts around 10:31:01, 10:38:15, 10:44:59; two completed failures | ROOT CAUSE; CODE FIX; TEST; DOCUMENTATION | Independent shell lacked reliable failure semantics; d-i late failure returned to retry-capable control flow. Explicit status propagation and non-returning, main-menu-stopping supervision replace ordinary error exits. |
| syslog ~19035: Codex persistent ownership failure | SECONDARY FAILURE; CODE FIX; TEST | Missing managed packages link in CODEX_HOME. Candidate preparation and tmpfiles now agree. |
| syslog ~19389: Codex existing tree conflicts with pinned state | SECONDARY FAILURE; CODE FIX; TEST | Legitimate runtime-created state compared as immutable external content after partial publication. Define mutable paths narrowly, validate existing state without running its Git config, publish validated components atomically and release record last. |
| syslog 10:14:01-10:14:03: CD-ROM Release failure / update status 100, post-base hook 100; cleanup later at 10:15:25 | ROOT CAUSE; CODE FIX; TEST | First target update preceded apt-setup normalization. The bootstrap-base APT waypoint now normalizes and writes a signed network source before update. Status 100 remains fatal. |
| syslog 10:26:34: CrowdSec bouncer API key command before config.yaml | ROOT CAUSE; CODE FIX; TEST | pkgsel package-list order did not guarantee configured LAPI state. Install/validate engine first, then bouncer; reuse its generated key and verify authentication on first boot. |
| installer.log Mullvad package configuration: AppArmor reload failure, package installed | EXPECTED INSTALLER-CHROOT CONDITION; CODE FIX; TEST | Vendor script explicitly tolerates activation failure. Require profile provenance and offline parsing; defer only actual kernel loading to required checked boot unit. |
| CUDA InRelease ignored, signature/key errors; explicit insecure source message | SECURITY ISSUE; CODE FIX; TEST; DOCUMENTATION | Source-local authentication bypass accepted unverified metadata. Replace with dedicated keyring/full fingerprint and default signature/freshness checks. Wrong-key/unsigned/weak/expired and tampered-package fixtures reject. |
| installer.log mesa-utils-extra repeatedly considered missing despite installed provider | SECONDARY FAILURE; CODE FIX; TEST | Package-presence checker ignored virtual Provides. Query installed providers rather than repeatedly repairing a satisfied request. |
| Finish normalization named 95/99 after stock 95umount | SOURCE ROOT CAUSE; CODE FIX; TEST | Renumber installed normalization to 94zz, validate target next, guard all finish hooks and exit-11 handoff. |
| Kernel/initrd repairs after final GRUB generation | SOURCE ROOT CAUSE; CODE FIX; TEST | Move repair/signing before boot-pair checks and final menu generation. Require populated initrds and signature records at target validation. |
| arm64 arch class exists but CPU autodetection only accepts Intel/AMD | SOURCE ROOT CAUSE; CODE FIX; TEST | Add explicit generic-arm64 CPU policy without x86 microcode/platform flags. Existing profiles are retained. |
| New lifecycle helpers requiring stat/setsid/timeout would fail on real udeb | REPAIR VALIDATION FINDING; CODE FIX; TEST | Verified Debian udeb build configuration, replaced stat with checked numeric ls and setsid/timeout with proc-based supervision/bounded child execution. |
| GRUB firmware/NVRAM write warning in syslog (~18668-18669) | NEEDS HARDWARE VALIDATION | Retain existing removable fallback and signed boot-chain logic; validate boot artifacts. Unit tests cannot certify actual firmware fallback or enrolled trust. |
| Installer kernel module version differs from target kernel; signing/depmod diagnostics | EXPECTED NAMESPACE DIFFERENCE; TEST | Do not substitute running installer kernel for target kernel versions. Target kernel/initrd and signing checks are mandatory. |
| chroot service start/reload denied by policy, system bus unavailable | EXPECTED/SAFE INSTALLER-CHROOT CONDITION | Do not start target services in installer namespaces. Validate installation/configuration and use checked first-boot dependencies where required. |
| Btrfs -R runtime-feature deprecation warning | BENIGN WARNING; DOCUMENTATION | Existing selected filesystem features still supported; no cosmetic change to on-disk layout. |
| FAT CP850 conversion warning followed by internal conversion | BENIGN WARNING | Formatter supplied internal conversion and succeeded; no failure suppression added. |
| Secure Boot partition alignment / swap-size tolerance diagnostics | BENIGN WHEN WITHIN VALIDATED TOLERANCE; TEST | Existing byte/alignment checks retained; disk selection explicitly handles sysfs 512-byte units on 4Kn hardware. |
| CA rehash, pre-existing home, apt autoremove suggestions, package trigger messages | BENIGN DIAGNOSTIC | No blanket stderr filtering or fake success introduced. |
| Multiple Debian/vendor suites and duplicate vendor-generated source observations | POLICY / DOCUMENTATION | Retain explicit mixed-suite pins and existing finish normalization rather than changing the entire system's suite. Live solver/vendor availability remains an acceptance requirement. |
| Original baseline profile provenance assertion fails | TEST METADATA DEFECT | Current hash in migration ledger did not match supplied baseline file. Correct current hash with explanation; retain original provenance, do not modify the profile to satisfy the test. |

No claimed full successful installation can be inferred from the supplied logs:
they end during a re-entered failed late phase. Local regression results are in
`validation/summary.json`; deployment acceptance remains distinct.
