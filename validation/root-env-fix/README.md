# Root-env fix evidence

`before.json` reproduces the old stat/mode failure with dummy secrets.
`credentials-tests.log` and `target-tools-tests.log` are focused successful checks.
`release-check.json` records old/new preservation and every payload member hash.
The authoritative complete run is ../summary.json and ../tests.log (305 tests).
This is not evidence of a real USB boot, real private initrd inspection, or package
installation on the user's hardware. Previous-release supplemental evidence is
separately labeled in ../previous-release/.
