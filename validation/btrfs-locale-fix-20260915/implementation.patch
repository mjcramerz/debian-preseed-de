--- a/d-i/forky/hosts/installer/layout-btrfs.env
+++ b/d-i/forky/hosts/installer/layout-btrfs.env
@@ -61,9 +61,10 @@
 # Keep /boot conservative for shim/GRUB readers and avoid newer ext4 metadata
 # features that have historically caused compatibility pain on small boot tiers.
 MKFS_EXT4_BOOT_OPTS="-F -L ${FS_LABEL_BOOT} -m 1 -b 4096 -I 256 -i 16384 -O has_journal,extent,huge_file,flex_bg,dir_index,dir_nlink,extra_isize,sparse_super,large_file,filetype,uninit_bg,^64bit,^metadata_csum,^metadata_csum_seed,^orphan_file -E lazy_itable_init=0,lazy_journal_init=0,discard -J size=${SIZE_MKFS_EXT4_BOOT_JOURNAL_MB}"
-MKFS_BTRFS_ROOT_OPTS="-f -L ${FS_LABEL_ROOT} --checksum xxhash --data single --metadata dup --nodesize 16384 --features extref,skinny-metadata,no-holes --runtime-features free-space-tree"
-MKFS_BTRFS_HOME_OPTS="-f -L ${FS_LABEL_HOME} --checksum xxhash --data single --metadata dup --nodesize 16384 --features extref,skinny-metadata,no-holes --runtime-features free-space-tree"
-MKFS_BTRFS_OPT_OPTS="-f -L ${FS_LABEL_OPT} --checksum xxhash --data single --metadata dup --nodesize 16384 --features extref,skinny-metadata,no-holes --runtime-features free-space-tree"
+# Since btrfs-progs 6.3, free-space-tree belongs to --features as well.
+MKFS_BTRFS_ROOT_OPTS="-f -L ${FS_LABEL_ROOT} --checksum xxhash --data single --metadata dup --nodesize 16384 --features extref,skinny-metadata,no-holes,free-space-tree"
+MKFS_BTRFS_HOME_OPTS="-f -L ${FS_LABEL_HOME} --checksum xxhash --data single --metadata dup --nodesize 16384 --features extref,skinny-metadata,no-holes,free-space-tree"
+MKFS_BTRFS_OPT_OPTS="-f -L ${FS_LABEL_OPT} --checksum xxhash --data single --metadata dup --nodesize 16384 --features extref,skinny-metadata,no-holes,free-space-tree"
 MKFS_XFS_DATA_OPTS="-f -K -L ${FS_LABEL_DATA} -m crc=1,finobt=1,reflink=0,rmapbt=1,bigtime=1,inobtcount=1 -n ftype=1 -i size=512,sparse=1,nrext64=1"
 MKFS_XFS_POOL_OPTS="-f -K -L ${FS_LABEL_POOL} -m crc=1,finobt=1,reflink=1,rmapbt=1,bigtime=1,inobtcount=1 -n ftype=1 -i size=2048,sparse=1,nrext64=1"
 MKFS_EXT4_VAR_TMP_OPTS="-F -L ${FS_LABEL_VAR_TMP} -m 0 -b 4096 -I 256 -i 8192 -O has_journal,extent,huge_file,flex_bg,metadata_csum,64bit,dir_index,dir_nlink,extra_isize,sparse_super,large_file,filetype,uninit_bg -E lazy_itable_init=0,lazy_journal_init=0,discard -J size=${SIZE_MKFS_EXT4_VAR_TMP_JOURNAL_MB}"
--- a/d-i/forky/scripts/common/target.sh
+++ b/d-i/forky/scripts/common/target.sh
@@ -163,36 +163,52 @@
   command -v chroot >/dev/null 2>&1 && [ -d "$target_root" ]
 }
 
-target_exec() {
+target_exec() (
+  # Scope normalization to this call: do not alter the live installer's locale.
+  # Clear individual categories as well as LC_ALL so a target tool that unsets
+  # LC_ALL cannot reactivate stale installer settings. Use shell builtins here;
+  # /usr/bin/env below is the TARGET's tool, not an installer dependency.
+  # Set LC_ALL first: Bash recomputes its locale as categories are unset.
+  LC_ALL=C
+  LANG=C
+  export LANG LC_ALL
+  unset LANGUAGE \
+    LC_CTYPE LC_NUMERIC LC_TIME LC_COLLATE LC_MONETARY LC_MESSAGES \
+    LC_PAPER LC_NAME LC_ADDRESS LC_TELEPHONE LC_MEASUREMENT LC_IDENTIFICATION \
+    LOCPATH NLSPATH GCONV_PATH IT_LANG_OVERRIDE
+
   target_root=$(target_root_dir)
 
   case "$target_root" in
     /target|/target/)
       if command -v in-target >/dev/null 2>&1; then
-        (
-          unset \
-            DEBCONF_DB_REPLACE \
-            DEBCONF_REDIR \
-            DEBCONF_FRONTEND \
-            DEBCONF_NONINTERACTIVE_SEEN \
-            DEBCONF_SYSTEMRC \
-            DEBCONF_PIPE \
-            DEBCONF_USE_CDEBCONF \
-            DEBCONF_DEBUG \
-            DEBCONF_NOWARNINGS \
-            DEBCONF_TERSE \
-            DEBIAN_FRONTEND
-          # The installer locale archive may not match the upgraded target
-          # libc. Do not carry it across this boundary or depend on locale-gen.
-          unset LOCPATH LANGUAGE
-          LC_ALL=C
-          export LC_ALL
-          in-target --pass-stdout /usr/bin/env \
-            HOME=/root USER=root LOGNAME=root LANG=C.UTF-8 LC_ALL=C.UTF-8 \
-            XDG_CONFIG_HOME=/root/.config XDG_CACHE_HOME=/root/.cache \
-            XDG_DATA_HOME=/root/.local/share XDG_STATE_HOME=/root/.local/state \
-            "$@"
-        )
+        unset \
+          DEBCONF_DB_REPLACE \
+          DEBCONF_REDIR \
+          DEBCONF_FRONTEND \
+          DEBCONF_NONINTERACTIVE_SEEN \
+          DEBCONF_SYSTEMRC \
+          DEBCONF_PIPE \
+          DEBCONF_USE_CDEBCONF \
+          DEBCONF_DEBUG \
+          DEBCONF_NOWARNINGS \
+          DEBCONF_TERSE \
+          DEBIAN_FRONTEND
+        # chroot-setup.sh otherwise resets LANG from installer debconf. Its
+        # setup/cleanup commands must use C too, not an unavailable locale.
+        IT_LANG_OVERRIDE=C
+        export IT_LANG_OVERRIDE
+        # Retain in-target's Debconf passthrough/proxy environment (no env -i).
+        # Defend the target boundary again in case the bridge adds locale state.
+        in-target --pass-stdout /usr/bin/env \
+          -u LANGUAGE -u LC_CTYPE -u LC_NUMERIC -u LC_TIME -u LC_COLLATE \
+          -u LC_MONETARY -u LC_MESSAGES -u LC_PAPER -u LC_NAME -u LC_ADDRESS \
+          -u LC_TELEPHONE -u LC_MEASUREMENT -u LC_IDENTIFICATION \
+          -u LOCPATH -u NLSPATH -u GCONV_PATH -u IT_LANG_OVERRIDE \
+          HOME=/root USER=root LOGNAME=root LANG=C.UTF-8 LC_ALL=C.UTF-8 \
+          XDG_CONFIG_HOME=/root/.config XDG_CACHE_HOME=/root/.cache \
+          XDG_DATA_HOME=/root/.local/share XDG_STATE_HOME=/root/.local/state \
+          "$@"
         return
       fi
       ;;
@@ -213,7 +229,7 @@
   fi
 
   installer_fatal "neither chroot nor in-target is available for target command execution"
-}
+)
 
 target_command_failure_state_path() {
   printf '%s/state/last-target-command.failure\n' "$(installer_runtime_dir)"
