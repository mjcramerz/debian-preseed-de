{
  "initial_complete_validation": "attempt-1/summary.json",
  "initial_failures": [
    {
      "test": "test_actual_generated_http_bootstrap_under_busybox_at_every_boundary",
      "observed": "Bootstrap succeeded; aggregate HTTP GET count was 6 instead of 5. All three source/payload artifact counts were one.",
      "resolution": "No transport behavior or numeric assertion was changed. Five follow-up strict runs, including original test order, and the final complete validator passed. The assertion now includes the path counter in any failure message. The cause of the earlier extra request was not established."
    },
    {
      "test": "test_desktop_pipeline_fetches_and_installs_for_all_profiles",
      "observed": "An adjacency assertion ignored the existing Kanshi policy installation between resctl-bench installation and target asset staging.",
      "resolution": "Require exactly one resctl-bench installation before asset staging, without requiring adjacency. Production pipeline remains byte-identical to the upload."
    }
  ],
  "intermediate_validation_before_signal_cleanup": "attempt-2/summary.json",
  "signal_cleanup_regression": "signal-cleanup-regression.json",
  "final_complete_validation": "summary.json",
  "additional_repeat_evidence": [
    "bootstrap-repeats.log",
    "bootstrap-original-order.log"
  ]
}
